# Production dependencies for Equip Detail Discord Activity (FastAPI + MongoDB)

fastapi==0.110.1
uvicorn==0.25.0

# Env + HTTP
python-dotenv>=1.0.1
httpx>=0.27.0

# MongoDB
motor==3.3.1
pymongo==4.5.0

# Data validation
pydantic>=2.6.4

# Discord interactions signature verification (ed25519)
cryptography>=42.0.8

# Optional (safe to keep): form parsing if you later add file uploads / forms
python-multipart>=0.0.9
